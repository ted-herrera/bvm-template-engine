{
  "version": 2,
  "builds": [
    { "src": "public/**", "use": "@vercel/static" },
    { "src": "server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/demos/(.*)", "dest": "/public/demos/$1" },
    { "src": "/(.*)", "dest": "server.ts" }
  ]
}
